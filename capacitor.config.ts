import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.github.derickfelix.chessclock',
  appName: 'Chess Clock',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
